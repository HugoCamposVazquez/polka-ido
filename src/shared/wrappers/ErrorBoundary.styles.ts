import { sideColor11 } from '../../utils/colorsUtil';
import { styled } from '../../utils/css';

export const errorPageContainerStyle = styled.cssStyle`
  padding: 0.94rem;
`;

export const reloadLinkStyle = styled.cssStyle`
  text-decoration: underline;
  color: ${sideColor11};
  font-weight: bold;
`;

export const additionalInfoStyle = styled.cssStyle`
  margin-top: 0.75rem;
`;
