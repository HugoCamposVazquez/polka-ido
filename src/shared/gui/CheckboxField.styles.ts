import { sideColor3 } from '../../utils/colorsUtil';
import { styled } from '../../utils/css';

export const checkboxStyle = styled.cssClassName`
  .ant-checkbox-checked .ant-checkbox-inner {
    background-color: ${sideColor3};
  }
`;
